- `[/]` Initialize Task
- `[ ]` Update SimLab auto-import macro to inject the file-polling `QTimer` listener loop.
- `[ ]` Create `MeshingPage` UI component in `gui/app.py`.
- `[ ]` Add Global Mesh Control Bar (Type/Size/Apply All).
- `[ ]` Add Component List grid mapping components to mesh controls and specific scripts.
- `[ ]` Implement `_run_mesh` logic to dynamically modify scripts via regex and save to `_cache/mesh_jobs/`.
- `[ ]` Wire UI components (Open in SimLab unlocks Meshing tab).
- `[ ]` Test end-to-end execution.
